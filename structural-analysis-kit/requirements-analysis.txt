# Optional analysis environment; exact versions observed on the laptop.
# Preflight itself has no third-party dependencies. OpenSees/Torch are unnecessary for metadata QA.
numpy==2.4.2
scipy==1.17.1
matplotlib==3.10.8
