# Setup verification

- verified laptop profile accepted
- unset desktop paths rejected
- unset SSD mount rejected
- path traversal rejected
- source output overlap rejected
- solver failure and collapse labels retained separately
- missing dataset represented explicitly
- all device profiles parse

A real metadata preflight ran on the laptop. It did not run OpenSees or perform full-dataset QA. Desktop and SSD execution remain unverified until their local paths are configured.
