Set up this local task as my structural-engineering research analysis workspace using GPT-6 Astra. The desktop generates the primary research data; an external SSD carries stable dataset copies for analysis on either device. Existing laptop copies are only partial setup evidence.

Use `config/ssd.local.json` on the laptop or `config/desktop-ssd.local.json` on the desktop when analyzing SSD data, with the mounted dataset_base and this device's local project/interpreter paths checked first. Read SSD_WORKFLOW.md in that mode. Otherwise use `config/desktop.local.json` for desktop-local data or `config/laptop.local.json` for the existing partial laptop copies. Read `AGENTS.md`, `README.md`, `DATA_CONTRACT.md`, and `WORKFLOW.md`. These JSON profiles are analysis inputs, not Codex model configuration.

For this first run, validate the selected profile, inspect the local project/data roots and interpreter, and run `scripts/preflight.py` using the configured interpreter. If the selected desktop or SSD profile has unset paths, identify the exact fields to fill and stop only the path-dependent work. Do not substitute laptop paths. Discover likely calibrated/adaptive roots under the confirmed research outputs and present their plan metadata before adding them to a comparison. Do not infer set roles solely from folder names.

Produce a concise readiness report covering available roots, manifest-versus-file coverage, schema versions, metric-definition risks, missing dependencies, and the next useful analysis. Preserve generated inputs and use this workspace's dated outputs folders. Do not launch OpenSees, regenerate datasets, refresh source manifests, train a surrogate, install packages, or create scheduled monitoring during this setup pass.

For subsequent analysis requests, carry out the requested QA/QC, EDA, run triage, structural-response validation, calibration/adaptive-set comparison, or clean export using WORKFLOW.md. Continue autonomously on routine work, show evidence for findings, and distinguish physical extremes, solver failures, data defects, and censoring. Keep every reported metric tied to its definition and source run.
