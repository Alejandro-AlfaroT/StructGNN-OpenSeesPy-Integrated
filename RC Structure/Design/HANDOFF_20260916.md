# Handoff — 16 September 2026 (desktop → laptop)

Paste this file into the assistant session on the laptop before continuing.
It carries the repo state, the working conventions, what was done, what is
open, and the exact next commands. Every number here comes from the commits
and outputs named; nothing is transcribed from memory.

## 1. Repo state

- Repository: `StructGNN-OpenSeesPy-Integrated`, branch `main`, HEAD
  `82413573` ("Repair the merge and harden the verification/export tooling;
  fix the PROBE declaration date"). Local main was **6 commits ahead of
  origin/main** when this was written — push from the desktop, pull on the
  laptop, before anything else.
- Working tree clean at HEAD. Design schema:
  `rc_smrf_candidate_v8_directional_hoops` (`Design/Design_Driver.py`).
- `GENERATION_RELEASE_READY = False` (`Design/SMRF_Qualification.py:22`).
  Nothing generates until the engineer lifts it. No assertion flag has been
  set. No dataset run, calibration, NTHA or SAP application run was launched.
- Tests: from `RC Structure/`, `python -m unittest discover -s tests` → 397
  pass; from `RC Hybrid Surrogate Model/`, same command → 27 pass.
- Not in git (`RC Structure/outputs/` is ignored): the review artifact
  `outputs/smrf_evidence_review_20260913/` (design.json 64 MB +
  evidence_summary.md, regenerated on HEAD: 20x20 f'c 8 / 10x18, 1087 pass /
  0 fail / 7 open), verification smoke roots (`design_verification_smoke*`,
  `dv_repair_check`), sweep folders, pilot smoke. Regenerate on the laptop
  when needed (commands in §6); do not copy live output roots between
  machines.

## 2. Environment on the laptop

- Windows / PowerShell. Conda env `OpPy` (Python 3.12):
  `conda env create -f "RC Structure/environment-generation.yml"` if it does
  not exist. Interpreter path on the desktop was
  `C:/Users/andro/anaconda3/envs/OpPy/python.exe`; adjust.
- Run everything from `RC Structure/` (imports assume it).
- Assistant conventions that held on the desktop (keep them):
  - The Write tool refuses base-repo paths; write to the scratchpad then
    `cp`, or patch with a Python script. Bash heredocs with nested quotes
    break — put patch code in a file.
  - Commit only when asked; the user works directly on `main`; nothing is
    pushed by the assistant.
  - Verification flags are engineering assertions, never set by code. PROBE
    values are labelled in every artifact's request identity and are not a
    certification; never flip a flag to make a probe pass.
  - No "Done" labels in open-work lists. Keep the full `design.json`
    evidence trail (no trimming). Every code change to a design module
    invalidates cached `design.json` (source hashes are in the request
    identity) — use a fresh output root after any change.
  - Two review lanes were used: Astra (OpenAI) cross-checks pasted by the
    user, and a Codex repair pass (`Design/REPAIRS_20260916.md`). Every
    finding was reproduced before it was fixed, and every fix is a test.

## 3. What was done (2026-09-13 → 16)

Documents of record, in reading order:
`Design/SMRF_METHODOLOGY.md` (the method, by section),
`Design/SMRF_REVIEW_20260913.md` (four cross-check rounds and their
resolutions, newest first), `Design/REPAIRS_20260916.md` (the Codex repair
pass), `Design/DESIGN_VERIFICATION_RUN.md` (multi-machine run instructions).

Summary of the overhaul:

1. **Loads/demands (ASCE 7-22):** ELF with derived SDC, site class, risk
   category, 5% accidental torsion with Ax from drift runs, Cd drift, ACI
   6.4.2 live patterns, roof loads. Floor slab as a plate FE on flexible
   beams; reactions and interface couples transferred to the frame (couples as
   in-element force pairs, arm 1 in) and verified against a coupled
   slab-frame model per design (`SMRF_Coupled_Comparison`, v2 with pattern +
   mesh sensitivity).
2. **Section search that closes on per-joint results:** drift → deeper beam;
   capacity shear → wider then deeper; SCWB / joint shear / column shear →
   larger column; SCWB steel floor capped at ρ 4%; `max_section_iter=10`.
3. **Capacity design at every joint (ACI 318-19 ch. 18):** Mpr at 1.25fy,
   beam Ve from a joint-face free body over spans and live cases with
   physical drop weight, column shear, joint shear by joint kind (corner /
   edge_x / edge_y / interior, both axes), through-bar 20db, hook
   development, splice feasibility; SCWB per joint at the factored axial
   envelope, no roof exemption. Column hoop legs chosen per direction
   (`a96c54ad`).
4. **Column strength:** two uniaxial φP–M surfaces + Bresler contour
   (exponent 1.5); axial cap φPn,max enforced whatever the moment; every P–M
   sweep is an upper envelope cut at the cap with one point there.
5. **Beam-plus-slab strengths:** slab mats in the 6.3.2 flange as layers,
   strain compatibility; at exterior ends credited only if a standard hook
   (25.4.3.1) develops them in the perimeter beam, else neither mat in either
   sign (`terminated_undeveloped` hogging /
   `flange_concrete_undeveloped_bars` sagging), per-end hinge strengths.
6. **Slab reinforcement:** Wood-Armer strip actions → uniform #4/#5 mats;
   development, crack control, deflection, integrity bars, corners,
   perimeter hook per candidate spacing; shear basis = 318-19 8.4.3.1 one-way
   at beam faces, 8.4.4.1.1 two-way not applicable (beams on every edge,
   columns at intersections), beams' 100% share by 318-14 Table 8.10.8.1 at
   αf1·ℓ2/ℓ1 ≥ 1 (recorded as `alpha_f_l2_l1_min`).
7. **Cages:** hoop/crosstie arrangements per 18.7.5.2 / 25.7.2.3 /
   18.6.4.4, constructible legs only (`SMRF_Cage_Layout`).
8. **Model consistency:** IMK hinges yield at the design's composite
   strengths, asymmetric and per end; column hinges off the nominal surface
   (refuse a column whose gravity estimate is outside it); ρsh from the
   saved hoops; per-end spring yield rotation in the plastic-rotation
   post-processing.
9. **Evidence integrity:** qualification recomputes capacity design, slab
   strengths and the coupled comparison from the record (sha256-signed) and
   consumes only recomputed objects; tamper tests; declarations validated
   (site class / risk category exact strings; invalid DemandPolicy refused at
   design time).
10. **Tooling:** `Design/Verify_Designs.py` (plan-faithful design-only run,
    splittable across machines, artifact identity + requalification on
    reuse, OS leases, preserved locks), `Design/Evidence_Summary.py` (the
    review document, every number computed), `Design/Export_SAP2000.py` +
    `Design/Compare_SAP2000.py` (independent verification, export-only —
    not yet validated in SAP), `Design/Verification_Integrity.py`, refused
    designs terminal in the generator, compiled samples bound to status.json
    by SHA256.

Last thing fixed (in `82413573`): `Verify_Designs.probe_config` passed the
`datetime.date` class as the declaration date after the repair pass made
`--probe-date` mandatory — every PROBE design would have been refused. Fixed
and tested; verified end to end on one plan case (design → cached on relaunch
after identity check + requalification → counted by `--summarize-only`).

## 4. Current numbers

- Review case (3x3 bays x 10 ft, 8 stories x 10 ft, sdc_d_high): columns
  20x20 f'c 8, #7 4T/4B/3S, hoops #4-4L @ 3; beams 10x18 f'c 4, #5 4T/4B,
  hoops #4-3L @ 3; slab 5 in, #4 @ 10 both faces both axes. T1 0.990 s. 1087
  pass / 0 fail / 7 open (= the 7 IndependentVerification flags).
- Plan smoke on HEAD: `case_0002` (2x5x8, 12x15 ft, sdc_e) → 30x30 f'c 5 /
  14x26, accepted under PROBE, 1.9 min, 100 MB. (Designs moved vs the
  Sept 14 smoke because of the directional-hoop / 0.95 DCR-band commits.)
- Verification cost: ~2–8 min per case design-only, 100–180 MB each →
  150 cases ≈ 5–6 h on 4 workers, ~20 GB.

## 5. Open — the engineer's calls (nothing here is code)

Three blocks in `Design/Config.py`, all part of the request identity:

- `SlabActionAssertions` (line ~258): `analysis_applicability_verified`,
  `all_floors_enveloped`, `load_pattern_envelope_verified`,
  `spatial_envelope_per_unit_width`, `twisting_moment_resolution_verified`,
  `zero_membrane_force_verified`, `verified`, `two_way_shear_path_assessed`
  + `asserted_by` / `assertion_date` (ISO) / `assertion_basis`.
- `DemandPolicy` (line ~292): `declared_by` / `declaration_date` /
  `declaration_basis` (all three or none); `site_class` and `risk_category`
  must be the exact ASCE 7-22 spelling; occupancy, partition allowance,
  storage fraction, roof live, snow, wind, rain ponding, torsion ratio ≥
  0.05, live patterning.
- `IndependentVerification` (line ~331): `floor_hand_check_verified`,
  `strength_model_verified`, `detailing_model_consistency_verified`,
  `slab_column_local_steel_assessed`, `fire_resistance_scope_accepted`,
  `congestion_and_placement_accepted`, `floor_frame_compatibility_reviewed`
  + provenance.

Decisions those flags encode (the questions for the professor):
plate-to-bare-frame transfer idealization (coupled comparison ≤ 4.8%
verticals; gravity base moments 3–4% of governing, model gap 0.5–0.6% of
that); beam-supported two-way shear path basis; Bresler exponent 1.5 vs 1.0;
Vcol from mid-height inflection; SCWB with no roof exemption (why columns are
20x20 f'c 8 — the interior roof joint governs; lighter roof beams would be a
design policy); uniform #4/#5 mats (2h spacing governs; #3 would halve the
slab steel and the Mn⁻ enhancement); fire and 8.6.1.2 out of research scope
or not; the dataset's declarations (site class C, risk II, office, no wind)
across the five hazard sites; who does the hand check of one floor; nonlinear
target (60–70% going nonlinear) with stronger code-compliant frames; no
near-fault records while `sdc_e_near` is a design site; 2500 cases vs a
smaller verified set first.

## 6. Next steps, in order, with commands (run from `RC Structure/`)

1. Pull, create the env, run both test suites (§1).
2. Optional: regenerate the review artifact on the laptop so Astra/professor
   can read a current one:
   `python Design/Evidence_Summary.py --output-dir outputs/smrf_evidence_review_20260913`
   (designs the review case, ~5 min, then builds the IMK model and writes
   `evidence_summary.md`).
3. PROBE verification run over the plan (exercises the pipeline; not a
   certification). Choose ONE shared date for all machines, e.g.
   `--probe-date 2026-09-17`, and a fresh local root:
   `python Design/Verify_Designs.py --count 150 --plan-only`  (prints the plan
   SHA — must match on every machine)
   `python Design/Verify_Designs.py --count 150 --case-start 1 --case-end 75 --workers 4 --probe-assertions --probe-date 2026-09-17 --output-root <local>\dv150`
   (other machine: `--case-start 76 --case-end 150`). Resume by relaunching
   the same command; `--request-stop` to stop after in-flight cases. Merge
   per `Design/DESIGN_VERIFICATION_RUN.md` (copy only stopped runs, disjoint
   case folders, then `--summarize-only` on the merged root with the same
   count/seed/offset/date). A leftover `.design.json.lock` is never deleted
   automatically — confirm its owner exited first. `.launcher.lease` /
   `.worker.lease` files are normal and not to be deleted.
4. Fill the three blocks in `Design/Config.py` with the real assertions
   after the professor meeting; then the certification run is the same
   command **without** `--probe-assertions` / `--probe-date`, in a new root.
5. Lift `GENERATION_RELEASE_READY` (engineer's action, after step 4 shows
   the plan accepted).
6. Nonlinear pilot (a small set through `Ground_Motion_Main` with the
   release flag lifted; the Sept 14 smoke bypassed the gate in-process only),
   then the intensity-calibration refit, then the dataset run.
7. SAP2000 independent check (optional, parallel): `Design/Export_SAP2000.py`
   writes `frame` and `slab` `.s2k` variants from a design.json;
   `Design/Compare_SAP2000.py` compares SAP's exported tables. Not yet
   validated by a live SAP import — inspect point-moment directions, mesh,
   weight modifiers and the listed strength combinations first.

## 7. Known limitations to keep in view

- P-Delta and spatial mass mapping differ between the OpenSees design model
  and the SAP export; matching total weight does not establish modal
  equivalence.
- DCR agreement alone does not close the member-force, floor-load-path or
  detailing assertions — those are the flags in §5.
- Older verification artifacts (pre-`82413573`) are flagged stale by the
  resume/summary identity checks; keep them as history, do not relabel.
